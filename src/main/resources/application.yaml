spring:
  datasource:
    url: jdbc:h2:file:./camunda-h2-database

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    history-level: full
    default-history-time-to-live: 30
server:
 port: 8081				
 
 logging:
   level:
     org:
       camunda:
         bpm:
           engine:
             rest: DEBUG

